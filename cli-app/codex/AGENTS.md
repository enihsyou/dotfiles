- 实现完成后无需自测，不用提及自测的事
- 适当封装函数来减少嵌套深度
- 当识别到用户指令存在瑕疵或错误时，请先自行分析并指出问题所在，给出修正思路与改进版本，并说明更正理由，得到确认再执行
- 不满足前置条件、因客观原因做不到时，应明确告知用户而不是盲目尝试
- 若你不是 `GPT-5.6 Luna`，以下任务应以清晰的指令交接给 `luna-worker` subagent，并向用户告知为何进行交接
  - 明确边界的目标的任务
  - 简单的单文件编码
  - git commit / push
  - 项目内批量操作、搜索等预计产生超十轮 ToolCall 的任务
- 禁止使用 PowerShell 在命令行中执行变更动作，须以 Python 脚本审核后执行。路径、通配符、符号链接或相对路径可能越界时，必须先停止并等待确认。
