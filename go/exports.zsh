# shellcheck disable=SC2139

# 今后使用 gvm 管理 Go SDK 版本
# 这里的导出是为了在 GoLand 中设置一个所有SDK通用的 GOPATH
# (new) 注释掉改在 ~/.localrc 中设置
#export GOPATH=$HOME/.gvm/path
#export GOPATH=$HOME/Library/SDK/go/path
#export PATH="$PATH:$GOPATH/bin"
